#include <stdio.h>


int main(int argc, char ** argv) {
    while(1) {
        // the cpu is to read the byte at the memory address PC points to
        CPU * gba_cpu = newCPU();
        
    }
    return 0;
}